discord.py>=2.3,<3
requests>=2.28
beautifulsoup4>=4.11
pypdf>=4.0
python-dotenv>=1.0
# apply: optional. reportlab renders the generated Carta de Motivação as PDF
# (falls back to .txt if absent); playwright drives the `apply run` automation
# (also needs `playwright install chromium`). Neither is required for `!bolsas`
# or for plain `!apply <edital>` package preparation.
reportlab>=4.0
playwright>=1.40
